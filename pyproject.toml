[project]
name = "stone-detection-image-processing-only"
version = "0.1.0"
description = "Stone in kidneys detection project with image processing only"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.1.1",
]

[dependency-groups]
dev = [
    "ipykernel>=7.0.0",
]
